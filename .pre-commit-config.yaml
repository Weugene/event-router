repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.13.0
    hooks:
      - id: ruff
        name: ruff-import-sort
        args: [--fix, --select, I]
      - id: ruff
        name: ruff-lint
        args: [--fix]
      - id: ruff-format
